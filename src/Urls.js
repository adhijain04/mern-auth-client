// declaring api variables

// ------------------------- authentication ------------------------
const auth = {
    signUp: 'signup',
    signIn: 'signin',
    accountActivation: 'account-activation'
}

// exporting api call variables
export { auth };